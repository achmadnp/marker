export const PrimaryCheckbox = ({ label }) => {
  return (
    <label className="checkbox__label-primary">
      <input className="checkbox-primary" type="checkbox" />
      CHECK
    </label>
  );
};
